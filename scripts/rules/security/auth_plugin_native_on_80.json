{
  "id": "auth_plugin_native_on_80",
  "scope": "node",
  "priority": "P2",
  "title": "MySQL 8.0+ 默认 mysql_native_password",
  "rationale": "派生 SHA1 已弃用；8.4 起默认 disabled。",
  "trigger": "node.authPluginNativeOn80 == true",
  "groupKeyTpl": "auth_plugin_native_on_80",
  "descriptionTpl": "MySQL 8.0+ 默认认证插件仍为 mysql_native_password — 该插件派生 SHA1，已被弃用；8.4 起 mysql_native_password 默认 disabled",
  "currentValueTpl": "mysql_native_password",
  "recommendedValueTpl": "caching_sha2_password",
  "actionTpl": "新账号默认走 caching_sha2_password；存量账号灰度迁移；客户端驱动需 ≥ Connector/J 8.0、PyMySQL 1.0+",
  "sqlTpl": "-- my.cnf:\ndefault_authentication_plugin = caching_sha2_password\n-- 单账号迁移：\nALTER USER 'app'@'10.%' IDENTIFIED WITH caching_sha2_password BY '<pwd>';"
}
